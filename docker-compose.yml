services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - API_KEY=${API_KEY:-changeme}
      - JWT_SECRET=${JWT_SECRET:-changeme_too}
    volumes:
      - ./data:/app/data
